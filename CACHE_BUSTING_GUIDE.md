# Руководство по решению проблем с кешированием браузера

## Проблема
В процессе разработки может возникнуть ситуация, когда браузер показывает старую версию интерфейса (серый фон вместо обновленного дизайна) из-за агрессивного кеширования.

## Реализованные решения

### 1. Настройки Next.js (next.config.ts)
- Добавлены HTTP заголовки для принудительного отключения кеширования в development режиме
- `Cache-Control: no-cache, no-store, must-revalidate`
- `Pragma: no-cache`
- `Expires: 0`

### 2. Middleware (src/middleware.ts)
- Автоматическая установка заголовков кеширования для всех запросов
- Добавление timestamp для принудительного обновления

### 3. Meta теги в layout.tsx
- HTTP-Equiv meta теги для дополнительного контроля кеширования
- Динамический `last-modified` timestamp

### 4. Компонент CacheManager
- Кнопка "🔄 Cache Clear" в правом нижнем углу (только в development)
- Автоматическая очистка Service Worker кеша
- Принудительное обновление страницы с новыми параметрами

## Решение проблемы для пользователей

### Способ 1: Жесткое обновление (рекомендуется)
1. **В Chrome/Edge/Firefox:** `Ctrl + Shift + R` (или `Ctrl + F5`)
2. **В Yandex Browser:** `Ctrl + Shift + R` или `Ctrl + F5`
3. **На Mac:** `Cmd + Shift + R`

### Способ 2: Очистка кеша браузера
1. **Yandex Browser:**
   - `Ctrl + Shift + Del`
   - Выбрать "Изображения и файлы" 
   - Нажать "Очистить"
   - Обновить страницу

2. **Chrome:**
   - `Ctrl + Shift + Del`
   - Выбрать "Cached images and files"
   - Нажать "Clear data"

### Способ 3: Инкогнито/приватный режим
- Открыть сайт в режиме инкогнито для проверки актуальной версии
- `Ctrl + Shift + N` (Chrome/Yandex)
- `Ctrl + Shift + P` (Firefox)

### Способ 4: Developer Tools
1. Открыть DevTools (`F12`)
2. Правый клик по кнопке обновления
3. Выбрать "Empty cache and hard reload"

### Способ 5: Кнопка Cache Clear (development)
- В development режиме в правом нижнем углу есть кнопка "🔄 Cache Clear"
- Клик по ней принудительно очищает весь кеш и обновляет страницу

## Технические детали

### Для разработчиков
- Service Worker кеш очищается автоматически при загрузке страницы
- LocalStorage и SessionStorage очищаются при принудительном обновлении
- URL параметры `?_t=timestamp` добавляются для обхода HTTP кеша
- Middleware добавляет уникальные заголовки на каждый запрос

### Настройка для продакшна
В production режиме кеширование работает нормально:
- Статические файлы: `max-age=31536000, immutable`
- HTML страницы: `max-age=3600, s-maxage=86400`

## Профилактика
1. Всегда используйте жесткое обновление (`Ctrl+Shift+R`) при разработке
2. Проверяйте изменения в режиме инкогнито
3. Используйте кнопку Cache Clear в development режиме
4. При деплое новой версии, сообщайте пользователям о необходимости жесткого обновления

---

*Создано автоматически Claude Code для решения проблем с кешированием браузера в проекте Мебель-ИИ*